service:
  - docker

before_install:
- docker build . -t todo-app:test --target=test
- docker run --name travisImage -d -p 5000:5000 -e key -e token todo-app:test
- docker ps -a

script:  
- docker exec -it travisImage "pytest" --disable-pytest-warnings /srv/www/todo_app/tests

